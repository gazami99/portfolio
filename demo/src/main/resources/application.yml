spring:
  profiles:
    active: local


---
spring:
  config:
    activate:
      on-profile: local



  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/finance
    username: root
    password: root
  graphql:
    graphiql:
      enabled: true
    schema:
      printer:
        enabled: true
